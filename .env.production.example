# Production Environment Configuration
# Copy this file to .env and fill in the values

# ===========================================
# GitHub Container Registry
# ===========================================
# Your GitHub repository (e.g., username/remote-time-tracker)
GITHUB_REPOSITORY=your-username/remote-time-tracker

# Image tags (default: latest)
BACKEND_TAG=latest
FRONTEND_TAG=latest

# ===========================================
# Database Configuration
# ===========================================
POSTGRES_USER=rtt_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=remote_time_tracker
POSTGRES_PORT=5432

# Database SSL mode (disable, require, verify-ca, verify-full)
DB_SSLMODE=disable

# ===========================================
# Backend Configuration
# ===========================================
# Server
SERVER_PORT=8080
GIN_MODE=release

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_here_min_32_chars
JWT_EXPIRES_IN=24h

# Upload settings
UPLOAD_PATH=/app/uploads
MAX_UPLOAD_SIZE=10485760

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=https://your-frontend-domain.com

# ===========================================
# Frontend Configuration
# ===========================================
FRONTEND_PORT=3000

# API URL (set during build time in GitHub Actions)
# VITE_API_URL=https://your-api-domain.com/api/v1
# VITE_WEBSITE_DOMAIN=https://your-frontend-domain.com

# ===========================================
# Ports Mapping
# ===========================================
BACKEND_PORT=8080
FRONTEND_PORT=3000
